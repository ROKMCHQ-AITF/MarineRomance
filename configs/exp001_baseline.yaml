exp_name: exp001_baseline

model:
  backbone: tf_efficientnet_b0_ns
  num_classes: 50        # placeholder — update after running prepare_folds.py

train:
  folds: [0]
  epochs: 10
  batch_size: 32

feature:
  image_size: [128, 128]
  n_channels: 3
  channel_mode: repeat

wandb:
  tags: [baseline, 1fold]
  mode: disabled         # flip to online when ready to log
